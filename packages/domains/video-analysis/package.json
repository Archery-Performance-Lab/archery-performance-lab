{
  "name": "@apl/video-analysis",
  "version": "0.1.0",
  "private": true,
  "description": "Video Analysis Engine (M06) - shot sequence, shooting phase and pose analysis",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo \"No lint configured yet\"",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/test/**/*.test.js\"",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist dist-test"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^22.10.0"
  }
}
